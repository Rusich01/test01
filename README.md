// - create a new component with form;
// - component sould have one layout divided by sides:
// 1. left => form with three inputs (for example, name, surname, email)
// 2. right => preview (u sould be able to view the input data in live, for example JSON view of raw draft);
// - save actual draft to localStorage, in some data input => immidiate save data as draft to localStorage;
// - reinitialize data from localstorage once the page is reloaded;
// - show data in console/aler after submit + clear localStorage + clear all inputs;
// - mark-up with tailwind or module css or inline style;


    // створити; новий; компонент; із; формою;

    // компонент; повинен; мати; один; макет, поділений; на; дві;
    // ліва; сторона; форма; з; трьома; інпутами(наприклад, ім, я, прізвище, email);

    // права; сторона; попередній; перегляд(у, реальному, часі, відображати, введені, дані, наприклад, у, форматі, JSON, чернетка).

    //     ;

    // зберігати; поточну; чернетку; у; localStorage; при; введенні; даних, одразу; зберігати; їх; як; чернетку.

    //     ;

    // відновлювати; дані; з; localStorage; після; перезавантаження; сторінки.

    //     ;

    // після; натискання; Submit; показати; дані; у; console; або; alert, очистити; localStorage; і; всі; інпути.

    //     ;

    // розмітку; можна; зробити; з; допомогою; Tailwind; CSS, CSS; modules; або; inline - стилів.;
